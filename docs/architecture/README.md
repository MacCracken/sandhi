# Architecture notes

Non-obvious constraints, quirks, and invariants that a reader cannot derive from the code alone. Numbered chronologically — never renumber.

Not decisions (those live in [`../adr/`](../adr/)) and not guides. Items here describe *how the world is*, not *what we chose* or *how to do something*.

## Items

_Empty. Add numbered entries (`NNN-kebab-case.md`) when non-obvious invariants surface — e.g., keepalive teardown timing, mDNS listener lifecycle constraints, TLS handshake quirks that cross stdlib / sandhi boundary, or any primitive-composition surprise._
